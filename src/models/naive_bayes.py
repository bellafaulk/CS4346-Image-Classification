# naive bayes implementation
# learns probability of features with each class in training data
# predicts class with highest posterior probability in bayes' rule

def train(X, y): {
    pass
}

def predict(X): {
    pass
}